name: A basic flow with 2 jobs
on : push
jobs:
  job1:
    name: first job to run
    runs-on: ubuntu-latest
    steps:
     - name: Step-one
       uses: actions/checkout@v4
     - name: Step-two
       uses: ls -l
     - name: Step-three
       uses: pwd
  job2:
     name: Second job to run
     runs-on : Windows-latest
     steps:
       - name: Step one of job2
         uses: actopms/checkout@v4
       - name: Step two of job 2
         uses: pwd
